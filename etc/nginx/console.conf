set_real_ip_from 0.0.0.0/0; # change this with trusted addresses that are known to send correct replacement addresses
real_ip_header X-Forwarded-For;
real_ip_recursive on;
# change here your trusted subnet
# allow 10.0.0.0/8;
# allow 192.168.1.0/24;
# deny all;
